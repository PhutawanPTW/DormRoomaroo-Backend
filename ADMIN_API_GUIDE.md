# 🏢 DormRoomaroo Admin API Guide

## 📋 Overview
เส้น API สำหรับผู้ดูแลระบบ (Admin) ในการจัดการหอพักและอนุมัติหอพัก

**Base URL:** `http://localhost:3000/api/admin/dormitories`

---

## 🔐 Authentication
ทุกเส้น API ต้องใช้ Firebase ID Token:
```http
Authorization: Bearer [YOUR_FIREBASE_ID_TOKEN]
```

**สิทธิ์:** เฉพาะผู้ใช้ที่มี `member_type = 'admin'` เท่านั้น

---

## 📊 API Endpoints

### 1. ดูรายการหอพักทั้งหมด
```http
GET /api/admin/dormitories/all
```

**Response:**
```json
[
  {
    "dorm_id": 123,
    "dorm_name": "หอพัก ABC",
    "address": "123 ถนน...",
    "approval_status": "อนุมัติ",
    "submitted_date": "2024-01-01T00:00:00.000Z",
    "zone_name": "เขตมหาวิทยาลัย",
    "owner_username": "owner123",
    "owner_name": "เจ้าของหอพัก",
    "main_image_url": "https://..."
  }
]
```

### 2. ดูรายการหอพักที่รอการอนุมัติ
```http
GET /api/admin/dormitories/pending
```

**Response:** เหมือนกับ `/all` แต่แสดงเฉพาะหอพักที่มี `approval_status = 'รออนุมัติ'`

### 3. ดูรายละเอียดหอพักแต่ละตัว
```http
GET /api/admin/dormitories/:dormId
```

**Response:**
```json
{
  "dormitory": {
    "dorm_id": 123,
    "dorm_name": "หอพัก ABC",
    "address": "123 ถนน...",
    "approval_status": "อนุมัติ",
    "zone_name": "เขตมหาวิทยาลัย",
    "owner_name": "เจ้าของหอพัก",
    "owner_email": "owner@example.com",
    "owner_phone": "0812345678"
  },
  "images": [
    {
      "image_id": 1,
      "image_url": "https://...",
      "is_primary": true,
      "description": "รูปหลัก"
    }
  ],
  "room_types": [
    {
      "room_type_id": 1,
      "name": "ห้องแอร์",
      "bed_type": "เตียงเดี่ยว",
      "monthly_price": 3000,
      "total_rooms": 10,
      "available_rooms": 5
    }
  ],
  "amenities": {
    "ภายใน": [
      {
        "dorm_amenity_id": 1,
        "amenity_id": 1,
        "amenity_name": "แอร์",
        "location_type": "ภายใน",
        "is_available": true
      }
    ],
    "ภายนอก": [],
    "common": []
  },
  "members": [
    {
      "id": 1,
      "username": "member1",
      "display_name": "สมาชิก 1",
      "email": "member1@example.com",
      "start_date": "2024-01-01T00:00:00.000Z",
      "status": "กำลังอยู่"
    }
  ],
  "review_stats": {
    "total_reviews": 5,
    "average_rating": 4.2,
    "five_star": 2,
    "four_star": 2,
    "three_star": 1,
    "two_star": 0,
    "one_star": 0
  },
  "approval_history": {
    "approval_status": "อนุมัติ",
    "rejection_reason": null,
    "reviewed_date": "2024-01-01T00:00:00.000Z",
    "reviewed_by_name": "แอดมิน 1"
  }
}
```

### 4. ดูสมาชิกในหอพัก
```http
GET /api/admin/dormitories/:dormId/members
```

**Response:**
```json
{
  "dorm_id": 123,
  "members": [
    {
      "id": 1,
      "username": "member1",
      "display_name": "สมาชิก 1",
      "email": "member1@example.com",
      "phone_number": "0812345678",
      "photo_url": "https://...",
      "member_since": "2024-01-01T00:00:00.000Z",
      "start_date": "2024-01-01T00:00:00.000Z",
      "end_date": null,
      "status": "กำลังอยู่",
      "is_current": true,
      "request_date": "2024-01-01T00:00:00.000Z",
      "request_status": "อนุมัติ"
    }
  ]
}
```

### 5. ดูสถิติหอพัก
```http
GET /api/admin/dormitories/:dormId/stats
```

**Response:**
```json
{
  "dorm_id": 123,
  "basic_stats": {
    "total_room_types": 3,
    "total_rooms": 30,
    "available_rooms": 15,
    "total_amenities": 10,
    "current_members": 20,
    "total_members_ever": 50
  },
  "review_stats": {
    "total_reviews": 15,
    "average_rating": 4.1,
    "five_star": 5,
    "four_star": 7,
    "three_star": 2,
    "two_star": 1,
    "one_star": 0
  },
  "occupancy_stats": [
    {
      "month": "2024-01-01T00:00:00.000Z",
      "new_members": 5,
      "moved_out": 2
    }
  ]
}
```

### 6. อนุมัติ/ปฏิเสธหอพัก
```http
PUT /api/admin/dormitories/:dormId/approval
```

**Request Body:**
```json
{
  "status": "อนุมัติ",  // หรือ "ไม่อนุมัติ"
  "rejectionReason": "เหตุผลในการปฏิเสธ (ถ้าไม่อนุมัติ)"
}
```

**Response:**
```json
{
  "message": "สถานะการอนุมัติหอพักถูกปรับปรุงเรียบร้อยแล้ว"
}
```

### 7. แก้ไขหอพักโดยแอดมิน
```http
PUT /api/admin/dormitories/:dormId
```

**Request Body:**
```json
{
  "dorm_name": "หอพัก ABC (แก้ไข)",
  "address": "123 ถนนใหม่...",
  "dorm_description": "คำอธิบายใหม่...",
  "latitude": 16.123456,
  "longitude": 103.123456,
  "electricity_type": "มิเตอร์รวม",
  "electricity_rate": 8.5,
  "water_type": "มิเตอร์รวม",
  "water_rate": 25.0,
  "zone_id": 1,
  "approval_status": "อนุมัติ",
  "status_dorm": "เปิดใช้งาน"
}
```

**Response:**
```json
{
  "message": "อัปเดตข้อมูลหอพักเรียบร้อยแล้ว"
}
```

### 8. จัดการสถานะหอพัก (เปิด/ปิด)
```http
PUT /api/admin/dormitories/:dormId/status
```

**Request Body:**
```json
{
  "status_dorm": "ปิดใช้งาน"  // หรือ "เปิดใช้งาน"
}
```

**Response:**
```json
{
  "message": "อัปเดตสถานะหอพักเป็น 'ปิดใช้งาน' เรียบร้อยแล้ว",
  "status_dorm": "ปิดใช้งาน"
}
```

**หมายเหตุ:** เมื่อปิดใช้งานหอพัก ระบบจะ:
- บันทึกประวัติการย้ายออกของสมาชิกทั้งหมด
- อัปเดต `stay_history` เป็นสถานะ "ย้ายออก (หอพักปิดใช้งาน)"
- ถอดสมาชิกออกจากหอพัก (`residence_dorm_id = NULL`)

### 9. ลบหอพัก
```http
DELETE /api/admin/dormitories/:dormId
```

**Response:**
```json
{
  "message": "ลบหอพักเรียบร้อยแล้ว"
}
```

**หมายเหตุ:** การลบหอพักจะ:
- จัดการสมาชิกที่อาศัยอยู่ (บันทึกประวัติและถอดออก)
- ลบข้อมูลห้องพัก, ประเภทห้อง, สิ่งอำนวยความสะดวก
- อัปเดต `stay_history` เป็นสถานะ "หอพัก id X ถูกลบ"
- ลบรูปภาพและข้อมูลหอพัก

---

## 🚨 Error Responses

### 401 Unauthorized
```json
{
  "error": "Unauthorized: No token provided or malformed."
}
```

### 403 Forbidden
```json
{
  "error": "Forbidden: Admin privileges required."
}
```

### 404 Not Found
```json
{
  "message": "ไม่พบข้อมูลหอพัก"
}
```

### 500 Internal Server Error
```json
{
  "message": "เกิดข้อผิดพลาดในการดึงข้อมูลหอพักทั้งหมด"
}
```

---

## 📝 Testing Examples

### 1. ดูรายการหอพักทั้งหมด
```bash
curl -X GET "http://localhost:3000/api/admin/dormitories/all" \
  -H "Authorization: Bearer [YOUR_FIREBASE_TOKEN]"
```

### 2. อนุมัติหอพัก
```bash
curl -X PUT "http://localhost:3000/api/admin/dormitories/123/approval" \
  -H "Authorization: Bearer [YOUR_FIREBASE_TOKEN]" \
  -H "Content-Type: application/json" \
  -d '{"status": "อนุมัติ"}'
```

### 3. ปิดใช้งานหอพัก
```bash
curl -X PUT "http://localhost:3000/api/admin/dormitories/123/status" \
  -H "Authorization: Bearer [YOUR_FIREBASE_TOKEN]" \
  -H "Content-Type: application/json" \
  -d '{"status_dorm": "ปิดใช้งาน"}'
```

---

## 🔧 Frontend Integration

### 1. Admin Dashboard
- ใช้ `/all` เพื่อแสดงรายการหอพักทั้งหมด
- ใช้ `/pending` เพื่อแสดงหอพักที่รอการอนุมัติ
- ใช้ `/:dormId` เพื่อดูรายละเอียดแต่ละหอพัก

### 2. Approval Process
- ใช้ `/:dormId/approval` เพื่ออนุมัติ/ปฏิเสธหอพัก
- ใช้ `/:dormId/status` เพื่อเปิด/ปิดใช้งานหอพัก

### 3. Management
- ใช้ `/:dormId/members` เพื่อดูสมาชิกในหอพัก
- ใช้ `/:dormId/stats` เพื่อดูสถิติหอพัก
- ใช้ `/:dormId` (PUT) เพื่อแก้ไขข้อมูลหอพัก

---

## ⚠️ Important Notes

1. **Authentication:** ทุกเส้น API ต้องใช้ Firebase ID Token
2. **Authorization:** เฉพาะผู้ใช้ที่มี `member_type = 'admin'` เท่านั้น
3. **Data Safety:** การลบหอพักจะจัดการข้อมูลสมาชิกอย่างปลอดภัย
4. **Status Management:** การปิดใช้งานหอพักจะย้ายสมาชิกออกอัตโนมัติ
5. **Audit Trail:** ระบบจะบันทึกประวัติการเปลี่ยนแปลงทั้งหมด
